name: syllabuz_front
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  url_launcher: ^6.2.1
  intl: ^0.19.0
  file_picker: ^8.0.0+1  # Add this for file picking
  
  
  # API & HTTP
  http: ^1.5.0
  dio: ^5.3.2
  
  # State Management
  provider: ^6.1.5
  
  # Storage
  shared_preferences: ^2.5.3
  
  # Utils
  logger: ^2.6.1
  
  # Loading
  flutter_spinkit: ^5.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true